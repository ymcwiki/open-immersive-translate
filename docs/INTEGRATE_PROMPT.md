You are the INTEGRATOR for a Chrome MV3 bilingual translation extension. Six workstreams (A extract, B services/scheduler/cache, C render/observe, D rules, E ui, F features) were developed in parallel on branches ws/A..ws/F from the phase-0 scaffold and have just been merged into main (merge conflicts, if any, were resolved mechanically — re-check any file containing conflict markers or `<<<<<<<`). Read docs/DEV.md, CONTRACTS.md, and CONTRACT_REQUESTS.md first.

Your job: make the extension actually work end to end.

1. Resolve CONTRACT_REQUESTS.md: apply each requested contract/manifest change in src/shared/* or src/manifest.ts (e.g. web_accessible_resources for youtube-main, extra message types like getServices/testService/validateRule/openOptions), then update callers. Delete CONTRACT_REQUESTS.md entries as you satisfy them.
2. Wire src/background/index.ts for real: on install set defaults; handle getRule (matchRule with user rules from config), getConfig/setConfig, translate (scheduler.translateParagraphs streaming results over the Port), cancel, getServices, testService, validateRule, openOptions; context menus (翻译网页 / 翻译选中文本); chrome.commands → forward toggle messages to the active tab; config change broadcast.
3. Wire src/content/index.ts for real: load config + rule → inject styles → build a TranslationController: extractParagraphs → observeViewport → on visible send translate over Port with priority for visible → on results decode placeholders and renderTranslation → mutation + url-change re-scan → toggleTranslate / setMode / removeAll; auto-translate when hostname in alwaysTranslateSites or rule says so, never when in neverTranslateSites; same-language skip using lang.detectLang (implement detectLang for real with a small heuristic: CJK/Latin/Cyrillic/Arabic script ratios + common-word check for en/fr/de/es; do not add a dependency). Init the six features with a FeatureContext. Handle service-worker restarts: if Port disconnects, reconnect and resend pending ids.
4. Popup toggle must work from a fresh install with google service and no API key.
5. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` all green. Fix root causes, do not skip tests.
6. Write an end-to-end smoke test with Playwright (add as devDependency) that launches Chromium with the built dist/ loaded (`--load-extension`), opens a local fixture HTML page (tests/e2e/fixtures/article.html with a few English paragraphs, a nav of links, a code block), stubs network via a mock translation service selected in config (register a `mock` service in src/background/services/mock.ts that returns `[zh] ` + text, gated behind config.services.mock.enabled), triggers translation via the extension's toggle message, and asserts: each paragraph got exactly one `font[data-imt=target]`, the nav did not, the code block did not, and removeAll restores the DOM. Script: `pnpm e2e`.
7. Update README.md: what it is, how to build, how to load unpacked, how to configure an OpenAI-compatible key, how to write a site rule. Chinese.
8. Commit: `git add -A && git commit -m "phase2: integrate workstreams, e2e smoke test"`.

Rules: minimal code, no new abstractions beyond what is needed to wire things; do not rewrite workstream modules unless they are broken — fix bugs in place and keep their tests passing. Code/comments in English, UI strings zh-CN.
