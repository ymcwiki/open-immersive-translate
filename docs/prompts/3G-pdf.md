You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "~/Library/Application Support/Google/Chrome/<Profile>/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: G — PDF translation. Items F1 F2 F3 F4 F5. Directories: src/pdf/ (new), tests/pdf/.

Build a PDF reader page (src/pdf/index.html + index.tsx, Preact) using pdfjs-dist (pnpm add) with the worker bundled locally (no CDN; note the required web_accessible_resources / build config in CONTRACT_REQUESTS.md). Features:
- Open via ?file=<url> or a file picker (local PDF via FileReader → ArrayBuffer). Drag-and-drop too.
- Render pages with pdf.js canvas + text layer. Extract paragraphs from the text layer: group text items by page into lines by y, merge lines into paragraphs by vertical gap / font size / line-ending punctuation heuristics; keep reading order for two-column layouts by clustering x ranges. Export `extractPdfParagraphs(textContent, viewport)` as a pure function with unit tests on synthetic text-item fixtures.
- Translate paragraphs through the existing background scheduler (use the same translate Port protocol as the content script; import from src/shared/messages.ts) and render translations under each source paragraph as an overlay layer positioned by paragraph bbox (dual) or replacing (translation-only), with the same theme classes as the web renderer (import src/content/render/themes.css). Lazy: only visible pages.
- Toolbar: page nav, zoom, mode dual/translation, service select, target lang, "翻译全部", download bilingual PDF (F5: use pdf-lib (pnpm add) to draw translated text under each paragraph on a copy of the original; embed a CJK-capable font — bundle a small subset font file or fall back to a system font via opentype? Keep it simple: bundle NotoSansSC subset if size permits, otherwise document limitation).
- URL interception (F3): provide `src/pdf/intercept.ts` exporting a background helper that, when config.pdf.interceptLinks is true, uses chrome.webNavigation or tabs.onUpdated to redirect top-level navigations ending in .pdf (and content-type application/pdf where detectable) to the reader page; document the needed permissions in CONTRACT_REQUESTS.md.
- arXiv (F4): in the reader, if the URL is an arxiv.org/abs/ or /pdf/ link, normalize to the PDF URL.
- Config additions (request in CONTRACT_REQUESTS.md): pdf: { interceptLinks: boolean, mode, theme }.
