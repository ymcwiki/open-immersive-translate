You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "/Users/weyk/Library/Application Support/Google/Chrome/Profile 1/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: H — video subtitles. Items E2..E9. Directories: src/content/features/subtitle/ (new; you may refactor the existing youtube-subtitle.ts and youtube-main.ts INTO this directory), src/subtitle-file/ (new page), tests/subtitle/.

Build a generic subtitle engine plus site adapters:
- Core: `SubtitleEngine` that takes cue streams ({start,end,text}[]), batches by sentence (max 50 cues / 4000 chars), translates via ctx.translateText (existing FeatureContext) with cache, and exposes bilingual cues. Pre-translation mode (E3): translate the whole track when it loads; otherwise translate on a rolling window ahead of currentTime. Toggle via a message the integrator will bind to the toggleVideoSubtitlePreTranslation shortcut (describe in CONTRACT_REQUESTS.md).
- Renderer (E2, E9): a custom subtitle overlay positioned over the <video> (shadow root), configurable font size, colors, background opacity, position, mode (dual / translation-only / source-only), draggable, hides native cues where possible (track.mode = 'hidden'). Persist style in config.subtitle (request schema in CONTRACT_REQUESTS.md).
- Adapters (E1 upgraded, E4, E5, E6, E7): each adapter = { matches, hook(): cue source }. Implement: youtube (timedtext xhr/fetch intercept, MAIN world, already partly exists — move and generalize the MAIN-world interceptor into a shared injectable that supports URL patterns per adapter), netflix (intercept the ?o= timedtext TTML/DFXP responses and parse), primevideo (TTML), disneyplus/hbomax/hulu (best-effort via WebVTT/TTML request interception; mark as experimental), coursera/udemy/edx/khanacademy/ted/vimeo/linkedin (WebVTT fetch interception or <track> elements), bilibili (player subtitle JSON from api.bilibili.com/x/player/... intercept), twitter/facebook/dailymotion (WebVTT/<track>), generic (E7): any page whose <video> has <track kind=subtitles/captions> — read via TextTrack API cues. Write parsers for WebVTT, SRT, TTML/DFXP, YouTube json3, Bilibili json as pure functions with unit tests on fixture strings.
- Local subtitle file page (E8): src/subtitle-file/index.html+tsx: upload srt/vtt/ass, translate all cues via background, preview table, download bilingual/translated-only in the same format (write an ASS parser/serializer too; keep ASS style headers intact).
- Integration contract: export `initSubtitles(ctx)` from src/content/features/subtitle/index.ts replacing the old youtube init; list manifest needs (web_accessible_resources for MAIN-world script, host patterns) in CONTRACT_REQUESTS.md.
