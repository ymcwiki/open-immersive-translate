You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "/Users/weyk/Library/Application Support/Google/Chrome/Profile 1/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: K — side panel, AI writing, selection/input upgrades, options expansion. Items C5 C11 C12 C13 C15 C16 C18 C19 G10 G12. Directories: src/ui/ (popup, options, shared, sidepanel — new), src/content/features/selection-translate.ts, src/content/features/input-translate.ts, src/content/features/ai-writing/ (new), tests for these.

Implement:
- C18 side panel: src/ui/sidepanel/index.html+tsx registered via chrome.sidePanel (request manifest side_panel + permission in CONTRACT_REQUESTS.md). Tabs: 翻译 (free text translate with service/lang pickers, history), 对话 (chat with the selected AI service; a "解释这段" quick action receives selected text from the page via a message), 页面 (current page state, translate/untranslate, mode, service). Streams responses if the service supports streaming (check for an onPartial hook; fall back to non-streaming).
- C19 AI writing modal: a page-level modal (shadow root) opened by shortcut/menu with the current selection or input text: actions 总结 / 润色 / 翻译 / 生成建议 (3 suggestions) with editable prompts; insert result back into the focused input or copy.
- C11/C12/C13 selection upgrades: dictionary mode for single words (ask the AI service for phonetics/POS/definitions/examples with a dedicated prompt, render structured), TTS via speechSynthesis with language voice selection (auto-read option), trigger modes: icon-hover / icon-click / direct popup; per-site enable pattern.
- C15/C16 input upgrades: language code aliases (e.g. /en /ja /zh, configurable alias table), a target-language bar shown above the focused input after trigger, auto language mode (translate to English when typing Chinese and vice versa), configurable trailing trigger key + repeat count + timeout.
- C5 popup more-menu: 设置, 快捷键, 清除缓存, 反馈(GitHub issues URL placeholder), 打开侧边栏, 翻译本地 PDF (opens the pdf reader page path src/pdf/index.html — workstream G builds it), 翻译字幕文件 (src/subtitle-file/index.html — workstream H).
- G10 cache stats/clear wired to background messages getCacheStats/clearCache (they now exist after phase 2 — verify in src/shared/messages.ts; if absent, request).
- G12 i18n: add zh-TW and ja tables for all UI strings; language follows chrome.i18n.getUILanguage with override in config.
- Options page: generic service credential forms driven by a `serviceFields(serviceId)` descriptor (workstream I is adding it in src/background/services; until merged, define the expected shape locally in src/ui/shared/service-fields.ts and note it in CONTRACT_REQUESTS.md), sections for the new config areas: 字幕样式, PDF, 侧边栏, AI 写作, 划词详细设置, 语言规则 (always/never translate languages, mode by language/url patterns, theme patterns), 全局 CSS. Write to config with the new keys; document all new Config keys with zod snippets in CONTRACT_REQUESTS.md.
