You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "~/Library/Application Support/Google/Chrome/<Profile>/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: L — rules at scale, remote rules, search enhancement, distribution. Items G5 G7 G13 G14 G15 (and G4 quality). Directories: src/background/rules/ (builtin-rules.ts, new builtin-rules/*.ts split by category, remote-rules.ts), src/content/features/search-enhancement.ts, scripts/ (new), build config files for extra targets (vite.firefox.config.ts, vite.userscript.config.ts — new files only), tests/rules/.

Implement:
- G5: Port ≥100 site rules. Source: the reference default_config.json 'rules' array (769 entries; schema keys like matches, selectors, excludeSelectors, additionalExcludeSelectors, atomicBlockSelectors, stayOriginalSelectors, extraInlineSelectors, extraBlockSelectors, mutationExcludeSelectors, injectedCss, isTranslateTitle, paragraphMinTextCount, blockMinTextCount, shadowRootSelectors, urlChangeDelay, selectorMatches, translationTheme, etc.). Write scripts/port-rules.ts (run with tsx via pnpm dlx or add tsx devDep) that reads that JSON, maps its keys onto OUR Rule schema (drop unknown/Pro/telemetry keys; convert `key.add`/`key.remove` suffix semantics into our additional* fields), filters to a curated allowlist of ~120 high-traffic sites (news, dev, social, docs, academic, shopping, video, forums; include github, twitter/x, reddit, youtube, wikipedia, stackoverflow, arxiv, medium, hackernews, google, bing, duckduckgo, quora, discord, slack, telegram web, gmail, outlook, notion, figma docs, mdn, docs.python.org, npmjs, pypi, huggingface, openai/anthropic docs, substack, nytimes, bbc, guardian, reuters, bloomberg, economist, techcrunch, verge, wired, ars, amazon, ebay, aliexpress, steam, epic, imdb, netflix browse pages, linkedin, facebook, instagram, threads, mastodon, bluesky, pixiv text pages, nature, science, pubmed, sciencedirect, springer, ieee, acm, semanticscholar, scholar.google, chatgpt.com, claude.ai, gemini, perplexity, phind, v2ex, zhihu, juejin, csdn, bilibili text pages, douban, weibo, xiaohongshu, and more), and emits src/background/rules/builtin-rules/*.ts grouped by category with a generated index. Keep the 10 hand-written rules as overrides (they win). Add a test that every generated rule validates against validateRule and that a sample of URLs picks the expected rule. Commit the generated files.
- G7 remote rules: config.remoteRules: [{url, enabled}] fetched by background every 24h (alarms API — request permission) and cached in storage; merged between builtin and user rules; options UI hook documented in CONTRACT_REQUESTS.md (workstream K owns the UI).
- G13 search enhancement: on google/bing/duckduckgo result pages, when the query is in the target language, show a small bar offering to also search the translated query (translate via ctx.translateText) with links to open results in English; per-config enable.
- G14 Firefox: a vite config producing dist-firefox/ with MV3 manifest tweaks (background.scripts vs service_worker, browser_specific_settings gecko id, sidebar_action if side panel exists); script `pnpm build:firefox`. Verify with web-ext lint (pnpm add -D web-ext) in a test or a script; do not need to run Firefox.
- G15 userscript: a vite config bundling content script + an in-page config UI shim into a single .user.js with GM_xmlhttpRequest for cross-origin fetch (a small runtime adapter in src/userscript/ replacing chrome.runtime/storage with GM_* equivalents); `pnpm build:userscript`. Keep scope: page translation + popup-equivalent floating menu; PDF/subtitle pages excluded from the userscript.
