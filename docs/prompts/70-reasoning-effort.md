Add a configurable reasoning effort to the ChatGPT OAuth provider of this Chrome MV3 bilingual translation extension. Read src/background/services/chatgpt-oauth/{auth,service}.ts, the service-fields/options code that renders the ChatGPT card, src/shared/config.ts, and tests/background/services/chatgpt-oauth* first.

Reference behavior (hermes-agent, <path-to>/hermes-agent/agent/reasoning_effort.py and agent/auxiliary_client.py ~line 1615-1640, readable at ~/.hermes/hermes-agent on this machine): the Codex backend (chatgpt.com/backend-api/codex/responses) accepts a top-level `reasoning: { effort }` in the Responses body; it rejects `{"effort": null}`, so either omit the field or send a valid level. Wire vocab per model: gpt-5.5 accepts none|low|medium|high|xhigh; gpt-5.6* additionally accepts max; `minimal` is rejected by both. Clamp policy: keep a supported level verbatim, otherwise take the nearest WEAKER supported level.

Implement:
1. Config: `reasoningEffort` (translation main path) and `reasoningEffortAssistant` (side panel chat, dictionary, AI writing — any non-batch request) on the ChatGPT service config. Defaults: translation `low`, assistant `medium`. Values: none|low|medium|high|xhigh|max. Add zod schema + defaults + migration for existing configs (missing → defaults).
2. service.ts: every Responses request sends `reasoning: { effort: clampEffort(level, model) }` where `clampEffort` implements the vocab/clamp rules above in a small pure module `src/background/services/chatgpt-oauth/reasoning.ts` (exported: EFFORT_LADDER, supportedEfforts(model), clampEffort(level, model)). Translation batches use `reasoningEffort`; assistant/dictionary/writing requests use `reasoningEffortAssistant`.
3. Options: on the ChatGPT card add two selects 「翻译思考强度」 and 「助手思考强度」 with zh-CN labels (无 / 低 / 中 / 高 / 极高 / 最大) and a one-line hint that 翻译建议 低，追求速度选 无. Hide `max` unless the selected model is a gpt-5.6* model; if a stored value is unsupported by the current model, show the clamped value with a note.
4. Tests: clampEffort matrix (each level × gpt-5.5 / gpt-5.6-sol / gpt-5.4-mini / unknown model), request body contains the right effort for translation vs assistant paths, config migration fills defaults, options select writes config and hides max appropriately.
5. README: extend the ChatGPT section with a short 「思考强度」 paragraph (defaults, why low for translation, model-dependent max).
6. `pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm e2e` green. Commit: `feat(chatgpt): configurable reasoning effort`. Do not add a Co-Authored-By trailer.
