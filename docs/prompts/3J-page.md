You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "~/Library/Application Support/Google/Chrome/<Profile>/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: J — advanced page translation, styles, shortcuts. Items A11 A13 A14 A16 A17 A18 A20 A21 A22 A23 A24 A28 A29 A30 B2 B4 B5 B8 C1 C2 C3 C9. Directories: src/content/extract/, src/content/render/, src/content/observe/, src/content/controller/ (new — you own the page translation controller; the integrator currently has controller logic in src/content/index.ts: MOVE it into src/content/controller/ keeping index.ts as a thin entry, and describe the one-line change in CONTRACT_REQUESTS.md), src/background/rules/ (defaults only), tests for these.

Implement:
- A17 main-area detection: heuristic to find the main content container (article/main/[role=main], largest text density block, exclude nav/aside/footer) and a "translate main only" mode; A18 "translate to page end immediately" mode that bypasses viewport lazy loading with a concurrency cap; both switchable at runtime.
- A16/A29: config alwaysTranslateLangs/neverTranslateLangs + translationModeLanguagePattern: decide auto-translate and mode from detected page language. A13/A14: real language detection (script ratios + stopword lists for en/fr/de/es/it/pt/ru/ja/ko/zh/ar/vi/th; unit-tested with sample sentences), paragraph-level skip when same as target; A28 translationModeUrlPattern.
- A11 iframes: make sure the controller runs in subframes only when the frame has enough text (mainFrameMinTextCount) and reports to top via postMessage for toggle sync.
- A20 glossary with domain tags ({k,v,domain?}) and A21 context awareness: compute page title + first-N-words summary and pass as context in translate requests (the scheduler already accepts context; verify).
- A22 mask mode: theme 'mask' + a runtime toggle (translation blurred until hover) + message hook for shortcut; A23 editable translations: double-click a translation to edit inline (contenteditable), save to cache override (per-site map in storage) and re-apply on next load.
- A24: pre/likePre handling — split by lines for pre-like blocks when translating (advanceTransformPreTagNewLine equivalent), keep whitespace.
- A30: report page state (idle/translating/done/error, counts) to background for the action badge (message contract in CONTRACT_REQUESTS.md).
- B2 themes: dashedBorder solidBorder thinDashed nativeUnderline nativeDashed nativeDotted weakening blur; B4 custom font size/color/line-height via CSS variables from config; B5 per-site theme/mode patterns (translationThemePatterns: {theme: [urlGlobs]}); B8 global custom CSS string injected on every page.
- C1/C2/C3/C9: a `commands.ts` in controller mapping every command id (toggleTranslatePage, toggleTranslateTheWholePage, toggleTranslateTheMainPage, toggleOnlyTranslation, toggleTranslateToThePageEndImmediately, toggleTranslationMask, toggleMouseHoverTranslateDirectly, toggleVideoSubtitlePreTranslation, translateWithGoogle/Bing/DeepL/OpenAI/Claude/Gemini/Custom1-3) to controller actions; list the manifest commands block (Chrome allows only 4 suggested keys; others user-assignable) in CONTRACT_REQUESTS.md; hover-directly mode toggles hover translation without a modifier key.
- Keep all existing extract/render/observe tests passing; add tests for every new behavior.
