Add a "ChatGPT account (OAuth)" translation provider to this Chrome MV3 bilingual translation extension, replicating how hermes-agent (NousResearch) authenticates with a ChatGPT subscription and calls the Codex backend. Read docs/DEV.md, CONTRACTS.md, README.md, src/background/services/{base,index,openai-compatible}.ts, src/shared/{config,messages,types}.ts, and the options page code first.

REFERENCE IMPLEMENTATION (read these files; port the behavior, not the code):
- <path-to>/hermes-agent/hermes_cli/auth.py — constants near line 124-150 (CODEX_OAUTH_CLIENT_ID = "app_EMoamEEZ73f0CkXaXp7hrann", CODEX_OAUTH_TOKEN_URL = "https://auth.openai.com/oauth/token", DEFAULT_CODEX_BASE_URL = "https://chatgpt.com/backend-api/codex"); `_codex_device_code_login` (~line 8355): POST https://auth.openai.com/api/accounts/deviceauth/usercode {client_id} → {user_code, device_auth_id, interval}; user opens https://auth.openai.com/codex/device and enters user_code; poll POST https://auth.openai.com/api/accounts/deviceauth/token {device_auth_id, user_code} (403/404 = not yet, 200 = {authorization_code, code_verifier}); exchange POST oauth/token form {grant_type: authorization_code, code, redirect_uri: "https://auth.openai.com/deviceauth/callback", client_id, code_verifier} → {access_token, refresh_token, id_token}; `refresh_codex_oauth_pure` (~line 3997): form {grant_type: refresh_token, refresh_token, client_id}; 429 handling; `_import_codex_cli_tokens` (~line 4175): reading ~/.codex/auth.json shape {tokens:{access_token, refresh_token, account_id?}}; `_codex_access_token_is_expiring` (JWT exp claim, 120s skew).
- <path-to>/hermes-agent/agent/auxiliary_client.py `_codex_cloudflare_headers` (~line 1240): headers `originator`, `User-Agent`, and `ChatGPT-Account-ID` taken from the JWT claim `["https://api.openai.com/auth"]["chatgpt_account_id"]`.
- <path-to>/hermes-agent/agent/transports/codex.py (~line 570-600): request body for POST {base}/responses is the OpenAI Responses API shape {model, instructions, input:[{role:"user", content:[{type:"input_text", text}]}], store:false, stream:true}; the official backend expects streaming SSE (events like response.output_text.delta, response.completed, error).
- <path-to>/hermes-agent/hermes_cli/codex_models.py: model list probe GET https://chatgpt.com/backend-api/codex/models?client_version=1.0.0 and the static fallback list.

IMPLEMENT:
1. src/background/services/chatgpt-oauth/ : 
   - auth.ts: device-code login state machine (start → show code → poll → exchange → store), token refresh with 120s skew, JWT claim decode (account id, plan type, exp, email if present), logout, `importCodexCliAuth(json: string)` that accepts the pasted contents of ~/.codex/auth.json. Tokens live in chrome.storage.local under a dedicated key, never in exported config (extend the existing export redaction). Polling runs in the background service worker with chrome.alarms or a resilient setTimeout loop that survives restart by persisting {device_auth_id, user_code, startedAt} and resuming; 15-minute timeout.
   - service.ts: TranslationService "chatgpt" (name "ChatGPT 账号（OAuth）") extending the OpenAI-compatible batching/prompt logic (reuse the YAML id batching and prompt templates from openai-compatible.ts by extracting shared helpers if needed) but sending the Responses-API body to https://chatgpt.com/backend-api/codex/responses with headers Authorization: Bearer <access>, ChatGPT-Account-ID, originator: "bilingual-translator", User-Agent: "bilingual-translator/<version>", Accept: text/event-stream; parse SSE and concatenate output_text deltas; map 401 → refresh once then retry → auth error; 429 → rate_limit with Retry-After. Supports streaming via the existing onPartial hook if present. Model list from the models endpoint with cached fallback (default model: the first non-codex gpt-5.x mini-class model available, else "gpt-5.4-mini").
   - Register in services/index.ts and serviceFields() (no apiKey field; instead an "auth" field type the options page renders as the login card).
2. Messages: chatgptOauth.start / status / cancel / logout / importCli (add to src/shared/messages.ts) handled in background.
3. Options page: service card for ChatGPT shows: not logged in → 「登录 ChatGPT」 button → shows the user code (large, copy button) and a 「打开登录页面」 link to https://auth.openai.com/codex/device, live status while polling, success shows account email/plan/expiry, 「退出登录」; a collapsible 「从 Codex CLI 导入」 textarea for ~/.codex/auth.json contents; 「测试连接」 works with this service. Popup service picker lists it when logged in.
4. Tests (vitest, mocked fetch + mocked chrome.storage/alarms): device flow happy path, pending 403/404 polling, 429 backoff, exchange error, refresh success/failure, JWT decode, expiring check, CLI import validation, SSE parsing (multi-event, split chunks, error event), header construction, 401→refresh→retry once. Options card component tests for each state.
5. README: a section 「用 ChatGPT 账号登录（无需 API key）」 with the steps and the note that this uses the same device-code flow as Codex CLI / hermes-agent and that the tokens stay in local extension storage.
6. `pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm e2e && pnpm build:firefox && pnpm build:userscript` green (userscript may exclude this provider if GM fetch cannot stream; document). Update docs/FEATURE_PARITY.md with a new row D21 「ChatGPT 账号 OAuth 登录（设备码）」 ✅. Commit: `feat: ChatGPT OAuth (device code) provider`.

Rules: minimal, direct code; no new abstractions beyond a small shared SSE parser; UI strings zh-CN with en fallback via the existing i18n helper.
