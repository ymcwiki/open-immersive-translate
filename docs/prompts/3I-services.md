You are one of six agents working IN PARALLEL (phase 3) on a Chrome MV3 bilingual translation extension that already works end to end. Read docs/DEV.md, CONTRACTS.md, docs/FEATURE_PARITY.md, README.md, and skim src/ to learn the current architecture before writing code. The reference product is 沉浸式翻译 (Immersive Translate) 1.32.7; its unpacked files are readable at "~/Library/Application Support/Google/Chrome/<Profile>/Extensions/bpoadfkcbjbfhfodiogcnhhhpibjhbnh/1.32.7_0/" — use default_config.json and _locales there as a behavioral reference (do NOT copy its minified JS). HARD RULES: (1) Work only inside your workstream directories listed below, plus tests for them. (2) You may CREATE new files under src/shared/ named with your workstream prefix (e.g. src/shared/pdf-types.ts) but must NOT edit existing src/shared/*, src/manifest.ts, src/background/index.ts, src/content/index.ts, or package.json except: adding devDependencies/dependencies you truly need via `pnpm add`, and appending a clearly-delimited block to CONTRACT_REQUESTS.md describing exact edits the integrator must make to shared files, manifest, entry points, and the Config schema (give code snippets). Provide an `init`/`register` function in your area so wiring is a one-liner. (3) Implement fully with vitest unit tests; `pnpm typecheck && pnpm lint && pnpm test` green at the end. (4) Minimal, direct code; no speculative abstractions. (5) UI strings zh-CN with en fallback via existing i18n helper (add keys in your own i18n file if needed). (6) Update the Status column of your items in docs/FEATURE_PARITY.md (✅ only when implemented AND tested). (7) Commit: git add -A (never node_modules) && git commit -m "phase3:<letter> <summary>".

YOUR WORKSTREAM: I — translation services. Items D3 D5 D6 D7 D9 D10 D11 D12 D14 D15 D19 D20. Directories: src/background/services/ (add files; you may edit base.ts/index.ts/openai-compatible.ts in this directory), tests/background/services/.

Add adapters (each with fetch-mocked unit tests):
- gemini.ts (generateContent API, system_instruction, YAML batching like openai), deepl.ts (official free/pro endpoints, auth key, tag handling, formality option), bing.ts (free: fetch edge auth token from https://edge.microsoft.com/translate/auth, call api-edge.cognitive.microsofttranslator.com/translate; token cache/refresh), azure-translator.ts (key+region), volc.ts (Volcengine translate with HMAC-SHA256 signing via SubtleCrypto), tencent.ts (TMT with TC3-HMAC-SHA256 signing), baidu.ts (appid+key MD5 sign — implement MD5 locally, small), youdao.ts (appKey/secret SHA-256 sign), caiyun.ts, aliyun.ts (machine translation with HMAC-SHA1 signing), papago.ts, yandex-free.ts (session-less endpoint where possible; else mark limited), transmart.ts, niutrans.ts, openl.ts, azure-openai.ts (deployment URL + api-version + api-key).
- Presets (D12): a data file `presets.ts` listing OpenAI-compatible providers with baseUrl + default model + model list: deepseek, qwen (dashscope compatible-mode), kimi/moonshot, zhipu, siliconcloud, groq, openrouter, grok/xai, ollama (localhost:11434/v1), mistral, doubao/ark, hunyuan, lingyiwanwu, stepfun, qianfan, minimax. Each preset registers a service id that extends openai-compatible.
- D14: expose editable system + user prompt templates per AI service (config fields promptSystem/promptUser with variables {{from}} {{to}} {{title}} {{summary}} {{glossary}} {{text}}), plus "subtitle" and "selection" prompt variants; defaults mirror Immersive Translate's defaults in default_config.json (translationServices.ai.systemPrompt / prompt env) rewritten in your own words.
- D15: models list + free-text custom model per service; a `getModels(serviceId)` helper.
- D19: optional streaming for openai-compatible/claude/gemini (SSE parse), delivering partial text via an onPartial callback the scheduler can pass through; keep non-streaming default.
- D20: `supportsPair(from,to)` per service with language code mapping tables (each MT provider uses its own codes: e.g. baidu 'zh', deepl 'ZH-HANS', bing 'zh-Hans'); scheduler should be able to skip unsupported services — document the hook in CONTRACT_REQUESTS.md.
- Update ServiceConfig needs (region, appId, secret, deployment, apiVersion, formality, promptSystem, promptUser, models, stream) in CONTRACT_REQUESTS.md with the exact zod additions; until merged, read them via a local typed accessor with defaults so nothing breaks.
- Update the options UI contract: export `serviceFields(serviceId)` describing which credential fields each service needs so the options page can render forms generically (workstream K/E will consume it).
