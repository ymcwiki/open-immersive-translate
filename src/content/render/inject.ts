import type { Paragraph, Rule } from "../../shared/types";

/** Insert one translated paragraph and return its wrapper element. */
export function injectTranslation(
  paragraph: Paragraph,
  translatedText: string,
  rule: Rule,
): HTMLElement {
  // TODO(phase1:render): Restore placeholders and insert the target wrapper.
  void paragraph;
  void translatedText;
  void rule;
  throw new Error("NotImplemented");
}
